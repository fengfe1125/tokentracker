# TokenTracker v0.3.3

## 简体中文

修复状态栏标题在官方配额数据过期时显示 `~` 的问题。

- 状态栏标题不再显示过期标记 `~`。
- 展开的配额明细仍保留数据来源标记。
- 本地估算标记 `≈` 保持不变。

## English

Fix the stale-quota marker shown in the menu bar title.

- The menu bar title no longer shows `~` for stale official quota data.
- Expanded quota details still retain source markers.
- The local-estimate marker `≈` is unchanged.

macOS 14+ · Apple Silicon · Ad-hoc signed; not notarized.
